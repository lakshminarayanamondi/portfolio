const Header = () => {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 text-center py-2 bg-background/95 backdrop-blur border-b border-border/30 transition-colors duration-300">
      <h2 className="text-lg font-bold gradient-text">MONDI LAKSHMI NARAYANA</h2>
    </header>
  );
};

export default Header;